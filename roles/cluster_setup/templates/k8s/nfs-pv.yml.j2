apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ pv_name }}
spec:
  capacity:
    storage: 25Gi
  accessModes:
    - {{ pv_access_mode }}
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: /srv/sfw/{{ pv_name }}
    server: {{ k8s_control_plane_host }}
    readOnly: false
